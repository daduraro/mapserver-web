# Mapsever front-end

An [OpenLayers](https://openlayers.org/)-based webpage for collaborative point annotations of image-based maps.

## Setup

Dependencies are managed by [npm](https://www.npmjs.com/) and application is bundled using [webpack](https://webpack.js.org/).

```bash
npm install

# for dev format
npm run dev

# for the final build
npm run build
```

### Icons

The font-based icons were obtained from [Fontello](http://fontello.com/) with the `fontello.config.json` (see [fontello-cli](https://github.com/paulyoung/fontello-cli) for a command-based way to obtain them).

# Disclaimer

This is a very early-stage pet project. It can (and will) contain critical security issues.